# traffic_violation_dashboard_project
This repository contains code(EDA, Analysis/Visualization Dashboard )for Traffic violation dataset

Setup:

1. Clone the repo:
2. Create and activate virtual environment:
3. Install dependencies:
  Python ≥ 3.10 pandas SQLAlchemy psycopg2-binary streamlit plotly
4. Create a database and table upfront in postgresql
5. Setup PostgreSQL database in main.py and streamlit_app.py: Update credentials - username, password and your_db

Run Pipeline: 
1.Run full EDA pipeline using python3 main.py in terminal
2.Run streamlit_app.py using streamlit run streamlit_app.py in terminal
